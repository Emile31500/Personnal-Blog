{% extends 'base.html.twig' %}

{% block title %} {{ controller_name }} {% endblock %}

{% block body %}

    {% for article in articles %}
        
            <div class="row p-3 my-3" style="box-shadow: 0 0.4em 1em grey;">
                <div class="col-4">
                    <h3 class="fs-4"> {{article.title}}</h3>
                </div>
                <div class="col-8">
                    <p> {{article.content|slice(0, 200) }} ... </p>
                    <a class='btn btn-outline-primary' href="/articles/{{article.id}}"> Continuer à lire</a> 
                </div>
            </div>

    {% endfor %}

{% endblock %}
